node_modules/
dist/
server-dist/
# nac3-kikoe is its own repo + npm package (@yujinapp/nac3-kikoe); it lives
# here as a sibling working copy but must never be committed into yuemail.
nac3-kikoe/
.yuemail/
.yujin-forge/
.forge/
# Runtime vault data only (never the vault source code): the data files
# live in .yuemail/ anyway, but guard against a stray copy at repo root.
/vault.salt
/vault.json
signatures/
.env
.env.*
*.tsbuildinfo
.DS_Store
.vite/
coverage/
